ALTER TABLE drones.COMPANIES ADD COLUMN STATE_ID INT UNSIGNED NULL AFTER COUNTRY_ID, ADD INDEX (STATE_ID), ADD CONSTRAINT FOREIGN KEY (STATE_ID) REFERENCES drones.STATES (ID) ON DELETE NO ACTION ON UPDATE NO ACTION;

ALTER TABLE drones.COMPANIES CHANGE COLUMN USERS_ID USER_ID INT UNSIGNED NOT NULL;
