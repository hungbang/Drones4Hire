ALTER TABLE drones.PROJECTS ADD COLUMN SORT_ORDER INT NOT NULL DEFAULT 0 AFTER FINISH_DATE;

ALTER TABLE drones.PAID_OPTIONS ADD COLUMN RATING INT NOT NULL DEFAULT 0 AFTER CURRENCY;
UPDATE drones.PAID_OPTIONS SET RATING = 1 WHERE ID = 3;

UPDATE drones.PAID_OPTIONS SET RATING = 3 WHERE ID = 2;

UPDATE drones.PAID_OPTIONS SET RATING = 5 WHERE ID = 1;