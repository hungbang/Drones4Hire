import { MyProjectsPipe } from './my-projects.pipe';

describe('MyProjectsPipe', () => {
  it('create an instance', () => {
    const pipe = new MyProjectsPipe();
    expect(pipe).toBeTruthy();
  });
});
