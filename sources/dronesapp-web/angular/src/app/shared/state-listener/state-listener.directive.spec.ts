import { StateListenerDirective } from './state-listener.directive';

describe('StateListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new StateListenerDirective();
    expect(directive).toBeTruthy();
  });
});
