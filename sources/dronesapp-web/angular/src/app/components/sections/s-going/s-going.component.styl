.s-going
  padding 100px 0
  &__title
    font-size 3.6rem
    font-weight: 400
    margin-bottom 70px
    text-align: center
    text-transform: uppercase
    position: relative
    &::before
    &::after
      content: "";
      position: absolute;
      left: 50%;
      bottom: -25px;
      height: 3px;
      width: 20px;
      background-color: #8a8a8a;
    &::before
      transform: translateX( -50% ) rotate(45deg);
    &::after
      transform: translateX( -50% ) rotate(-45deg);